#!/bin/bash

set -e

echo test

dvar() {
  if [ -z "$debug" ]; then
    return
  fi
  echo "$: <$${!1}>"
}

dvar() {
  if [ -z "$debug" ]; then
    return
  fi
  echo ": <${!1}>"
}
dvar() {
  if [ -z "$debug" ]; then
    return
  fi
  echo "$1: <${!1}>"
}